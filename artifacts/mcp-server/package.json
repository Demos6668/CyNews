{
  "name": "@workspace/mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CyNews MCP server — exposes threat intelligence to AI agents via the Model Context Protocol",
  "scripts": {
    "dev": "node --env-file=../../.env --import tsx ./src/index.ts",
    "start": "node --env-file=../../.env --import tsx ./src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@workspace/db": "workspace:*",
    "drizzle-orm": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "^5.8.3"
  }
}
